# Locals are intentionally empty for the telemetry-observability collection:
# the collection is a pure pass-through to the opensearch-domain,
# managed-grafana-workspace, and lambda primitives. No literals are required.
